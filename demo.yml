
management:
  info:
    git:
      mode: full
      enabled: true
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      expose:
        include: all

message:  hello Jacksonville
