
management:
  info:
    git:
      mode: full
      enabled: true
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: "*"

message:  hello Atlanta
